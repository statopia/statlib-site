# Proof narrative — empiricalCDF_le_one

Root: **empiricalCDF_le_one** (lemma) `Statlib/EmpiricalProcess/DKW.lean:82` · topic `EmpiricalProcess`
Closure: 2 declarations across 1 files. Generated from `proof_graph.json` — no files were moved.

Reading order (foundations first, headline last):

  ◆ `empiricalCDF` — def · `Statlib/EmpiricalProcess/DKW.lean:63`  _(also used by 4: empiricalCDF_monotone, empiricalCDF_nonneg, dkw_inequality_axiom, …)_
· `empiricalCDF_le_one` — lemma · `Statlib/EmpiricalProcess/DKW.lean:82` **← headline**

## Dependency diagram

```mermaid
flowchart TD
  n0["◆ empiricalCDF"]
  n1["· empiricalCDF_le_one"]
  n1 --> n0
  class n1 headline;
  classDef headline fill:#ffe08a,stroke:#d4a017,stroke-width:2px;
  classDef axiomNode fill:#ffd0d0,stroke:#c0392b,stroke-dasharray:4 2;
```
